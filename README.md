# mangaDownload
# Requirement
  # install python
    you need to install python and set it to envirement variables
    check this link : https://www.python.org/downloads/
  # install required libraries
    in terminal run the following commands to install these 'requests, bs4, re'
    pip install requests
    pip install bs4
    pip install re